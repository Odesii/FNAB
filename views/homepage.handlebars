{{#each posts}}
    <div class="post">
        <h2>{{this.title}}</h2>
        <p>{{this.content}}</p>
        <small>Posted by {{this.user.username}} on {{this.createdAt}}</small>
    </div>
{{else}}
    <p>No posts found.</p>
{{/each}}
